const models = require('.');

const sendSystemNotification = async(client, threadId, messages) => {

}

class Chat{
    constructor(client, threadId){
        this.client = client;
        this.threadId = threadId;
    }
}